# Configuration Reference

Top-level keys:

- `organization`: metadata and runtime defaults
- `hierarchy`: board/departments/team definitions
- `agents`: explicit agent declarations

Validation is provided by `agent_builder.config.validator` and CLI command:

```bash
agent-builder validate --config <path>
```
